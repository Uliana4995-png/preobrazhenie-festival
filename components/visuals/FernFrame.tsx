'use client';

/**
 * Природное обрамление краёв секции: белые камни со мхом и сверкающие
 * капли воды. По решению организатора элемент отключён по всему сайту —
 * компонент оставлен как единая точка управления (используется в 8
 * местах кода), чтобы при необходимости вернуть эффект достаточно
 * включить его здесь, не трогая остальные файлы.
 */
export default function FernFrame(_props: { position: 'top' | 'bottom' }) {
  return null;
}
