/**
 * Феникс — по решению организатора отключён по всему сайту (используется
 * в 3 местах кода: заставка, кемпинг, главный экран). Компонент оставлен
 * как единая точка управления — чтобы вернуть эффект, достаточно
 * включить его здесь, не трогая остальные файлы.
 */
export default function Phoenix(_props: { className?: string; imageUrl?: string }) {
  return null;
}
